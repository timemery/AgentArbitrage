Hi [Host Name],

Thanks for the detailed breakdown. Your assessment of the 10-thread hard ceiling and the CPU contention on the single vCPU is spot on.

To clarify the user interaction patterns and provide the data you requested:

**1. User Interaction & Traffic Mix**
You are correct. The vast majority (~95%) of user traffic is "Browsing," which just queries the local SQLite database (`/api/deals`). These requests are fast but CPU-bound, meaning they are the ones suffering when the Ingestor hogs the CPU.
The blocking "AI Actions" (which take 5-8s) **only** happen when a user explicitly clicks "Get Advice" or uses the Chat feature. They do not happen automatically.

**2. Best Time Window for Testing**
The Smart Ingestor is scheduled to run **at the top of every minute** (e.g., 12:00:00, 12:01:00).
*   **Pattern:** It wakes up, fetches a batch of deals, processes them (CPU Spike), and then sleeps until the next minute.
*   **Duration:** The CPU spike typically lasts **5–15 seconds** depending on the batch size.
*   **Test Window:** You can check at **XX:00 to XX:15 seconds** of any minute to see the contention in action.

**3. Requested Latency Tables (p50 / p95)**
Here are the missing details from the report to help you calculate the concrete capacity number:

**Group A: Light (Browsing)**
*Static content or simple DB reads.*
| Endpoint | Est. p50 / p95 Latency |
| :--- | :--- |
| `GET /` | 20ms / 50ms |
| `GET /dashboard` | 150ms / 400ms |

**Group B: Medium (Data Filtering)**
*Complex SQL queries. CPU-bound.*
| Endpoint | Est. p50 / p95 Latency | Workload |
| :--- | :--- | :--- |
| `GET /api/deals` | **400ms / 1.5s** | High CPU. Sorts/Filters thousands of rows. |
| `GET /api/deal-count` | **200ms / 800ms** | Frequent polling (every 30s). |

**Group C: Heavy (The Bottlenecks)**
*Thread-blocking operations.*
| Endpoint | Est. p50 / p95 Latency | Constraint |
| :--- | :--- | :--- |
| `GET /api/ava-advice` | **5s / 8s** | Blocks 1 Thread. (User Triggered Only) |
| `POST /api/mentor-chat`| **5s / 8s** | Blocks 1 Thread. (User Triggered Only) |

**Next Steps:**
I agree that moving the Ingestor is the ultimate fix. For now, on this hardware, I am proceeding with applying `Nice=19` and `CPUSchedulingPolicy=idle` to the Celery worker to prioritize web requests during those congestion windows.

Let me know what the test results show regarding the queuing during the ingest window!

Thanks,
Tim